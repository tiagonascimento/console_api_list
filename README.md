"# console_api_list" 

## para executar 
 - cmd raiz do projeto docker-compose up --build -d
 - acompanha a execução do console pelo docker  
        msg :Buscando dados da API.... isso pode levar lguns minutos 
            Salvando os dados isso pode levar lguns minutos
            rocesso concluido
- apos essas msg, a base vai estar populada com os dados 

endereço da API para consulta : http://localhost/swagger/index.html